import './divisor.css';

/* eslint-disable react/prop-types */
function Divisor({ divisorTitle }) {
  return (
    <div>
      <h2>{divisorTitle}</h2>
      <hr />
    </div>
  );
}

export default Divisor;
